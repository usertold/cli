# Third-party notices

The distributed CLI executable does not currently bundle third-party runtime dependencies.

Development-only dependencies are listed in `package.json` and are not bundled into the npm executable.
